version: '3.8'

services:
  dca_showcase:
    build: .
    container_name: dca_showcase_demo
    restart: always
    ports:
      - "8501:8501"
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_HEADLESS=true
